import React from "react";
import { Row, Col } from "reactstrap";

function BookFormat({ title, subtitle, authors, link, description, image, Button }) {
  return (

      <Row>
        <Col>
        </Col>
      </Row>

  );
}

export default BookFormat;
