import React from 'react';

export default class Saved extends React.Component{
    render(){
        return(
            <div>
                This is the saved page.
            </div>
        )
    }
}